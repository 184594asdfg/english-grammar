<!-- 初中语法页面 -->
<view class="junior-container">
  <!-- 专业网课目录 -->
  <view class="professional-catalog">
    <!-- 课程头部信息 -->
    <view class="course-header">
      <view class="header-content">
        <view class="course-title">📚 初中语法精讲课程</view>
        <view class="course-subtitle">系统掌握英语语法基础</view>
        <view class="course-stats">
          <text class="stat-item">共{{learningPath.length}}课时</text>
          <text class="stat-item">已完成{{completedCount}}课时</text>
          <text class="stat-item">整体进度{{overallProgress}}%</text>
        </view>
        <view class="achievement-badge" wx:if="{{overallProgress >= 100}}">
          <text class="badge-icon">🏆</text>
          <text class="badge-text">语法大师</text>
        </view>
      </view>
      <view class="progress-ring">
        <view class="progress-circle" style="background: conic-gradient(#4CAF50 {{overallProgress * 3.6}}deg, #e0e0e0 {{overallProgress * 3.6}}deg);">
          <view class="progress-inner">
            <text class="progress-text">{{overallProgress}}%</text>
            <text class="progress-label">进度</text>
          </view>
        </view>
      </view>
    </view>

    <!-- 目录列表 - 嵌套式章节结构 -->
    <view class="catalog-list">
      <block wx:for="{{chapters}}" wx:key="id">
        <!-- 章节容器 -->
        <view class="chapter-container {{expandedChapters.indexOf(index) > -1 ? 'expanded' : 'collapsed'}}"
              style="{{expandedChapters.indexOf(index) > -1 ? 'height: auto;' : 'height: auto;'}}">
          <!-- 章节头部 - 可点击展开/收缩 -->
          <view class="chapter-header" bindtap="toggleChapter" data-chapter-index="{{index}}">
            <view class="chapter-info">
              <view class="chapter-number-wrapper">
                <text class="chapter-number">第{{item.level}}章</text>
                <text class="chapter-badge">{{item.lessons.length}}课时</text>
              </view>
              <text class="chapter-title">{{item.title}}</text>
              <text class="chapter-desc">{{item.description}}</text>
            </view>
            <view class="chapter-controls">
              <view class="chapter-progress">
                <text class="chapter-progress-text">{{chapterProgress[index] || 0}}%</text>
                <view class="progress-bar">
                  <view class="progress-fill" style="width: {{chapterProgress[index] || 0}}%;"></view>
                </view>
              </view>
              <view class="expand-icon">
                <text class="expand-arrow {{expandedChapters.indexOf(index) > -1 ? 'expanded' : ''}}">▼</text>
              </view>
            </view>
          </view>
          
          <!-- 章节内的课时列表 - 使用微信小程序条件渲染 -->
          <view wx:if="{{expandedChapters.indexOf(index) !== -1}}" class="chapter-lessons-expanded">
            <block wx:for="{{item.lessons}}" wx:for-item="lesson" wx:key="id">
              <view class="lesson-item {{lesson.locked ? 'locked' : ''}} {{lesson.current ? 'current' : ''}} {{lesson.completed ? 'completed' : ''}}"
                    bindtap="navigateToLearning" 
                    data-item="{{lesson}}"
                    style="animation-delay: {{index * 0.1}}s">
                
                <!-- 左侧状态指示器 -->
                <view class="lesson-status">
                  <view class="status-icon">
                    <text wx:if="{{lesson.locked}}" class="lock-icon">🔒</text>
                    <text wx:elif="{{lesson.completed}}" class="check-icon">✓</text>
                    <text wx:elif="{{lesson.current}}" class="play-icon">▶</text>
                    <text wx:else class="number-icon">{{index + 1}}</text>
                  </view>
                  <view class="status-indicator"></view>
                </view>
                
                <!-- 中间内容区域 -->
                <view class="lesson-content">
                  <view class="lesson-title-wrapper">
                    <text class="lesson-title">{{lesson.name}}</text>
                    <text wx:if="{{lesson.current}}" class="learning-badge">正在学习</text>
                  </view>
                  <text class="lesson-desc">{{lesson.description}}</text>
                  <view class="lesson-meta">
                    <text class="lesson-duration">⏱ 15分钟</text>
                    <text class="lesson-difficulty">📊 基础</text>
                  </view>
                </view>
                
                <!-- 右侧操作区域 -->
                <view class="lesson-action">
                  <view wx:if="{{lesson.locked}}" class="action-locked">
                    <text class="action-icon">🔒</text>
                    <text class="action-text">未解锁</text>
                  </view>
                  <view wx:elif="{{lesson.completed}}" class="action-completed">
                    <text class="action-icon">✅</text>
                    <text class="action-text">已完成</text>
                    <text class="action-subtext">{{lesson.progress}}%</text>
                  </view>
                  <view wx:elif="{{lesson.current}}" class="action-current">
                    <text class="action-icon">▶️</text>
                    <text class="action-text">继续学习</text>
                    <text class="action-subtext">{{lesson.progress}}%</text>
                  </view>
                  <view wx:else class="action-start">
                    <text class="action-icon">📚</text>
                    <text class="action-text">开始学习</text>
                    <view class="start-arrow">→</view>
                  </view>
                </view>
              </view>
            </block>
          </view>
          <!-- 折叠状态的空容器 -->
          <view wx:else class="chapter-lessons-collapsed"></view>
        </view>
      </block>
    </view>
  </view>
</view>